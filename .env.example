# Frontend env for Vite
# Use this to point the client to the backend (include /api if your reverse proxy uses it)
VITE_BACKEND_URL=http://localhost/api
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=test-project